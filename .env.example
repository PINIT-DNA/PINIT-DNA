# ─── Server ───────────────────────────────────────────────────────────────────
NODE_ENV=development

PORT=4000
API_PREFIX=/api/v1

# ─── Apache Tika ──────────────────────────────────────────────────────────────
TIKA_URL=http://localhost:9998

# ─── Database ─────────────────────────────────────────────────────────────────
# Used by Prisma at RUNTIME (app queries) — Transaction Pooler, port 6543
DATABASE_URL="postgresql://postgres.ogvbrncyheigcbbzleeg:Ashwithareddy2004@aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

DIRECT_URL="postgresql://postgres.ogvbrncyheigcbbzleeg:Ashwithareddy2004@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres"

# ─── File Storage ─────────────────────────────────────────────────────────────
# Allowed MIME types (comma-separated)
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/webp,image/tiff
# Max upload size in bytes (default: 20MB)
MAX_FILE_SIZE=20971520
# Temp directory for uploaded files before processing
UPLOAD_TEMP_DIR=./tmp/uploads

# ─── DNA Generation ───────────────────────────────────────────────────────────
# Version tag stamped into every DNA record
DNA_SCHEMA_VERSION=1.0.0

# ─── Layer 6 — LSB Steganography ──────────────────────────────────────────────
# Secret used to generate the hidden AI signature payload
# ROTATE THIS IN PRODUCTION — treat like a signing key
LSB_SIGNATURE_SECRET=change_me_in_production

# ─── Vault Storage ────────────────────────────────────────────────────────────
VAULT_MASTER_SECRET=PinitDNA_Vault_Master_Secret_2024
VAULT_STORAGE_DIR=./vault/encrypted

# ─── Supabase Storage (vault file persistence) ────────────────────────────────
# Get from: Supabase → Settings → API   (project: kqdqmimdqecensurjplh)
SUPABASE_URL=https://kqdqmimdqecensurjplh.supabase.co
# Public anon key — safe in client bundle; reads/writes governed by RLS.
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImtxZHFtaW1kcWVjZW5zdXJqcGxoIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODE1MTQ4NTEsImV4cCI6MjA5NzA5MDg1MX0.yKoRKKuNHdsSVLEob4ZJDbsZ9Mi-b_rZoViMwq-IBGY
# Service-role key — regenerate from the NEW project (Settings → API) for
# server-side vault writes that must bypass RLS. Leave blank to fall back to anon.
SUPABASE_SERVICE_KEY=

# ─── Rate Limiting ────────────────────────────────────────────────────────────
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# ─── Logging ──────────────────────────────────────────────────────────────────
LOG_LEVEL=debug

# ─── Smart Links — Public App URL ─────────────────────────────────────────────
PUBLIC_APP_URL=http://localhost:3000

SHARE_HMAC_SECRET=pinit-dna-smart-links-hmac-dev-secret-change-in-prod

# ─── Image Monitoring ────────────────────────────────────────────────────────
# BING_SEARCH_API_KEY=your_key_here