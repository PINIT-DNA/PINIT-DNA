/** Total layers in the PINIT-DNA fingerprint pipeline (L1–L15). */
export const TOTAL_DNA_LAYERS = 15;

/** Core + intelligence layers (L1–L10). */
export const CORE_DNA_LAYERS = 10;

/** Advanced protection layers (L11–L15). */
export const ADVANCED_DNA_LAYERS = 5;
