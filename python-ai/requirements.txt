# ─── PINIT Enterprise AI Service — Python 3.11 ───────────────────────────────
# Core API (existing — do not remove)
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.1
httpx==0.27.0
python-multipart==0.0.9

# ─── Embeddings & vector search (existing) ─────────────────────────────────
sentence-transformers==3.0.0
faiss-cpu==1.14.2
numpy>=1.26.4,<2.1.0

# ─── Computer vision ───────────────────────────────────────────────────────
Pillow>=10.4.0
opencv-python>=4.9.0.80,<5.0.0
scikit-image>=0.22.0,<0.25.0
ImageHash>=4.3.1
piexif>=1.1.3

# ─── Deep learning (optional at runtime — diagnostics warn if missing) ─────
torch>=2.2.0,<2.5.0
torchvision>=0.17.0,<0.20.0
transformers>=4.40.0,<5.0.0
timm>=0.9.16,<2.0.0

# ─── OCR ───────────────────────────────────────────────────────────────────
pytesseract==0.3.10

# ─── Document processing ───────────────────────────────────────────────────
PyMuPDF>=1.24.0,<2.0.0
python-docx>=1.1.0,<2.0.0

# ─── Audio / video (system ffmpeg/ffprobe also required on PATH) ───────────
ffmpeg-python>=0.2.0
librosa>=0.10.1,<0.11.0
soundfile>=0.12.1
